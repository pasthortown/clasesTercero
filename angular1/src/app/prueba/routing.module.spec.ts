import { Prueba\routingModule } from './prueba\routing.module';

describe('Prueba\routingModule', () => {
  let prueba\routingModule: Prueba\routingModule;

  beforeEach(() => {
    prueba\routingModule = new Prueba\routingModule();
  });

  it('should create an instance', () => {
    expect(prueba\routingModule).toBeTruthy();
  });
});
